# Дизайн-концепция — «Разрез»

## Метафора

«Ситуации → Стратегии → Идентичность → Бытие» — это не список и не орбиты.
Это **разрез**: то, что видно на поверхности, и то, что лежит под ней.

Графический язык страницы — **срез концентрических оболочек под линией поверхности**.
Горизонтальная волосяная линия = поверхность (то, с чем клиент приходит).
Под ней — четыре вложенные полуоболочки, от светлой внешней («Ситуации»)
к плотному ядру («Бытие»). Чем глубже слой, тем темнее его значение.

Объект появляется ровно три раза и каждый раз работает:

| Где | Роль |
|---|---|
| Hero | Портрет Елены стоит **на линии поверхности**; оболочки уходят вниз за ним — тихая экспозиция идеи |
| Блок уровней | **Интерактивный селектор**: слой = уровень, клик по слою раскрывает уровень, раскрытый уровень подсвечивает слой |
| Финальный CTA | Только внутреннее ядро — марка четвёртого уровня |

Никаких орбит, шаров, свечений и колец. Реализация — SVG + CSS, без WebGL,
без canvas, без сторонних библиотек.

## Цвет — шкала глубины

Фирменные цвета сохранены полностью. Добавлен один более глубокий оттенок
того же синего, чтобы шкала имела дно.

| Токен | Hex | Роль |
|---|---|---|
| `--surface` | `#f7fafd` | поверхность, светлые страты |
| `--pale` | `#eef6fd` | фирменный фон (без изменений) |
| `--line` | `#d5eafa` | фирменная линия (без изменений) |
| `--sky` | `#6bb5ec` | фирменный голубой — **только сигнал**: активное состояние, focus, текст на тёмном |
| `--blue` | `#2e497c` | фирменный синий — primary, третий слой |
| `--core` | `#0d1b30` | ядро, тёмная страта (новый, тот же тон) |
| `--ink` | `#12233d` | заголовки |
| `--body` | `#3a4a63` | основной текст (9.1 : 1) |
| `--quiet` | `#66748c` | подписи (4.7 : 1) |
| `--on-core` / `--on-core-quiet` | `#e8f1fa` / `#9db4d0` | текст на ядре (16 : 1 / 8.1 : 1) |

Голубой выведен из текстовых ролей — там он давал 2.2 : 1.

**Ритм страт (сверху вниз):**
`surface` → `white` → **`core`** → `surface` → **`core`** → `core (footer)`.
Страница буквально темнеет к низу: скролл = погружение. Тёмная страта стоит
там, где текст говорит о четвёртом уровне, и там, где нужно решение.

## Типографика

Пара с реальной кириллицей, self-hosted (`assets/fonts/`, subset cyrillic+latin,
4 файла woff2, ~232 KB, `font-display:swap`). Google Fonts CDN убран.

- **Literata** — экранный редакционный serif с переменной оптикой. Заголовки,
  лид, утверждения, врезки. Даёт «редакционный», а не «продуктовый» регистр.
- **Golos Text** — кириллический гротеск Paratype. Тело, интерфейс, подписи,
  цифры (`tabular-nums`).

Шкала:

| Роль | Гарнитура | Размер | Интерлиньяж |
|---|---|---|---|
| h1 | Literata 400 | `clamp(2.5rem, 5vw, 4.4rem)` | 1.06 |
| h2 | Literata 400 | `clamp(1.9rem, 3.4vw, 3rem)` | 1.1 |
| statement (утверждение) | Literata 400 | `clamp(1.3rem, 2vw, 1.75rem)` | 1.3 |
| лид | Literata 300 | `clamp(1.15rem, 1.6vw, 1.4rem)` | 1.55 |
| h3 | Golos 600 | 1.2rem | 1.3 |
| тело | Golos 400 | 1.0625rem (17px) | 1.72 |
| подпись | Golos 400 | .8125rem | 1.55 |
| метка | Golos 600 uppercase .1em | .72rem | 1 |

Меры строк: тело **62ch**, лид **46ch**, врезки **34ch**, утверждения **28ch**.
Uppercase-разрядка применяется только к меткам в 1–3 слова.

## Композиция

12-колоночная асимметричная сетка, `--maxw:1240px`, `--gutter:clamp(20px,5vw,72px)`.
Контент привязан к левому краю; центрирование остаётся ровно в одном месте —
финальный CTA.

**Ось глубины** — сквозная волосяная линия в левом жёлобе, непрерывная через все
страты, с засечкой в начале каждой секции. Заменяет случайные декоративные волоски.

Плотность чередуется намеренно:
плотная страта уровней → воздушная врезка → плотный тёмный регистр следствий →
воздушный финал.

Карточек нет. Разделение — линиями и плоскостями. Радиус скругления — 2 px
(техническая деталь). Единственная круглая форма на странице — оболочки разреза,
где круг мотивирован.

## Взаимодействие

Блок четырёх уровней:

1. **Видеть модель целиком** — залипающий разрез + постоянная сводка слева.
2. **Понять различия** — название + подзаголовок каждого уровня видны всегда.
3. **Углубиться** — `<details name="levels">`: нативный эксклюзивный аккордеон,
   клавиатура и семантика из коробки, **работает без JavaScript**.
4. **Вернуться к сравнению** — диаграмма не уезжает; закрытие возвращает к сводке.

JS — только усиление: синхронизация подсветки слоя, клик по слою, автоскрытие
мобильной плиты, reveal. Без JS страница полностью функциональна.

`prefers-reduced-motion` отключает reveal, плавную высоту и любые переходы.
`:focus-visible` — 2 px `--sky` + 2 px offset, виден на всех стратах.

## Иерархия призывов

| Точка | Стиль | Почему |
|---|---|---|
| Header | тихая ссылка с подчёркиванием | не конкурирует с hero |
| Hero | сплошная primary `--blue` | единственный главный |
| Конец уровней | вторичная, встроена в вывод блока | продолжение мысли, не баннер |
| Конец `depth` (на ядре) | outline `--sky` на тёмном | другой контекст, другой вес |
| Финал | крупная светлая на ядре | точка решения |
| Мобильная плита | скрывается, когда виден финальный CTA | без дублирования |
